import React from 'react';

const outputFile = (props) => {
    return(
        <div>
            <p>{props.name}</p>
            <p>{props.age}</p>
        </div>
    )
    
};

export default outputFile;